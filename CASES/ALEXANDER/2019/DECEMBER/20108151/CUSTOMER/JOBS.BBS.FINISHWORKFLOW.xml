<?xml version="1.0" encoding="UTF-8"?>
<jobs>
   <metadata>
      <row>
         <version>12.3.1</version>
      </row>
   </metadata>
   <general_attributes>
      <row>
         <minimum_ae_version>11.2</minimum_ae_version>
         <auto_deactivation>J</auto_deactivation>
         <child_flags>00000000000000000000000000000000</child_flags>
         <ert>1</ert>
         <platform>UNIX</platform>
         <last_runtimes>AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA</last_runtimes>
         <max_parallel_action>1</max_parallel_action>
         <mrt_time>000000</mrt_time>
         <name>JOBS.BBS.FINISHWORKFLOW</name>
         <type>JOBS</type>
         <inherit_output_filter>N</inherit_output_filter>
         <queue>CLIENT_QUEUE</queue>
         <description>Wird als letztes in Standardjobplänen ausgeführt</description>
         <versioning_id>1171148950</versioning_id>
      </row>
   </general_attributes>
   <scripts>
      <row>
         <process><![CDATA[:SET &PARENT_NAME# = SYS_ACT_PARENT_NAME()
:PUT_ATT ARCHIVE_KEY1="&PARENT_NAME#"
:SET &YEAR# = SYS_DATE(YYYY)
:SET &LYEAR# = SUB(&YEAR#,1)
:SET &LYEAR# = FORMAT(&LYEAR#)

# zip options:
# -v verbose
# -r recursive
# -9 best compression
# -m move (delete source)
# -o latest-time


cd &WORKDIR#
cd ..
!Alle Verzeichnisse, die mit einer Zahl beginnen und Enden und 14 Stellen haben (Timestamps).
!Die aktuellsten 5 in der Liste nicht verarbeiten
echo "### Alte Unterverzeichnisse in $(pwd) zippen. Die aktuellsten 5 bleiben stehen:"
ls -r1 |grep "^[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]$" |awk 'NR>5' "$f" |while read subdir; do
  YEAR=$(echo $subdir |cut -c1-4)
  if [ ! -d $YEAR ]; then
    mkdir $YEAR
  fi
  echo "### Unterverzeichnis $subdir nach $YEAR/$subdir.zip zippen"
  zip -vrom9j $YEAR/$subdir.zip $subdir
  RC=$?
  if [ $? -ne 0 ]; then exit $RC; fi
  rm -rvf $subdir
done

cd &LOGDIR#
cd ..
!Alle Verzeichnisse, die mit einer Zahl beginnen und Enden und 14 Stellen haben (Timestamps).
!Die aktuellsten 5 in der Liste nicht verarbeiten
!Log Dateien aus aktuellem und letztem Jahr nicht zippen
echo "### Alte Unterverzeichnisse in $(pwd) zippen. Die aktuellsten 5 bleiben stehen:"
ls -r1 |grep -v "^&YEAR#" |grep -v "^&LYEAR#" |grep "^[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]$" |awk 'NR>5' "$f" |while read subdir; do
  YEAR=$(echo $subdir |cut -c1-4)
  if [ ! -d $YEAR ]; then
    mkdir $YEAR
  fi
  echo "### Unterverzeichnis $subdir nach $YEAR/$subdir.zip zippen"
  zip -vrom9j $YEAR/$subdir.zip $subdir
  RC=$?
  if [ $? -ne 0 ]; then exit $RC; fi
  rm -rvf $subdir
done

exit $RC]]></process>
      </row>
   </scripts>
   <job_attributes>
      <row>
         <activation_at_runtime>1</activation_at_runtime>
         <platform>UNIX</platform>
         <agent>HG.RH6_BATCH</agent>
         <login>LOGIN.BBSBATCH</login>
         <job_report_path>2</job_report_path>
         <priority>0</priority>
         <unix_shell>-bash</unix_shell>
         <unix_shell_options></unix_shell_options>
         <unix_type></unix_type>
         <unix_cmd></unix_cmd>
      </row>
   </job_attributes>
   <rollback_definitions>
      <row>
      </row>
   </rollback_definitions>
</jobs>
